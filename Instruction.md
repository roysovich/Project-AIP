# Инструкция по установке и запуску проекта "Аренда Строительной Техники"

Этот проект представляет собой веб-приложение для аренды строительной техники.
Технологии: Node.js, Express, Sequelize (PostgreSQL), HTML/CSS/JS.

## 📋 Требования

Перед началом убедитесь, что у вас установлены:
1.  **Node.js** (версии 14 или выше) - [Скачать](https://nodejs.org/)
2.  **PostgreSQL** (база данных) - [Скачать](https://www.postgresql.org/download/)
3.  (Опционально) **Docker** - для быстрого запуска в контейнере.

---

## 🚀 Способ 1: Быстрый запуск через Docker (Рекомендуется)

Если у вас установлен Docker, это самый простой способ.

1.  Откройте терминал в папке проекта `ConstructionRental`.
2.  Запустите проект командой:
    ```bash
    docker-compose up --build
    ```
3.  Дождитесь окончания сборки и запуска.
4.  Откройте в браузере: [http://localhost:3000](http://localhost:3000)

---

## 🛠️ Способ 2: Ручная установка (Без Docker)

### Шаг 1: Подготовка базы данных
1.  Установите и запустите PostgreSQL.
2.  Откройте pgAdmin или терминал psql.
3.  Создайте базу данных с именем `postgres` (или любым другим, но тогда обновите файл `.env`).
4.  Убедитесь, что у вас есть пользователь `postgres` с паролем `1`.
    *   Если пароль другой, откройте файл `.env` в папке проекта и измените `DB_PASSWORD=1` на ваш пароль.

### Шаг 2: Установка зависимостей
1.  Откройте терминал в папке проекта `ConstructionRental`.
2.  Установите необходимые библиотеки командой:
    ```bash
    npm install
    ```

### Шаг 3: Наполнение базы данных (Первый запуск)
Чтобы создать таблицы и добавить тестовую технику, выполните:
```bash
npm run seed
```
*Если возникнет ошибка подключения, проверьте, запущен ли PostgreSQL и правильные ли данные в `.env`.*

### Шаг 4: Запуск сервера
Запустите сервер командой:
```bash
npm start
```

### Шаг 5: Использование
Откройте браузер и перейдите по адресу:
[http://localhost:3000](http://localhost:3000)

---

## 📦 Как перенести проект на другой компьютер

1.  Скопируйте папку `ConstructionRental` целиком.
2.  **НЕ КОПИРУЙТЕ** папку `node_modules` (она весит много и создается автоматически).
3.  На новом компьютере следуйте инструкции "Ручная установка" (начиная с Шага 2).

## ☁️ Как залить на GitHub

1.  Создайте новый репозиторий на GitHub.
2.  В терминале проекта выполните:
    ```bash
    git init
    git add .
    git commit -m "Initial commit"
    git branch -M main
    git remote add origin <ССЫЛКА_НА_ВАШ_РЕПОЗИТОРИЙ>
    git push -u origin main
    ```
    *(Файлы `.env` и `node_modules` автоматически игнорируются и не попадут в репозиторий для безопасности).*


Если вы хотите сохранить проект на GitHub:

1.  Создайте новый репозиторий на GitHub.
2.  В папке проекта создайте файл `.gitignore` (если его нет) и добавьте туда:
    ```
    node_modules
    database.sqlite
    .env
    ```
3.  В терминале выполните команды:
    ```bash
    git init
    git add .
    git commit -m "Initial commit"
    git branch -M main
    git remote add origin <ССЫЛКА_НА_ВАШ_РЕПОЗИТОРИЙ>
    git push -u origin main
    ```

## Структура проекта

-   `server.js`: Главный файл сервера (Backend).
-   `models/`: Описание таблиц базы данных (Sequelize).
-   `public/`: Папка с файлами сайта (HTML, CSS, JS).
-   `database.sqlite`: Файл базы данных (создается автоматически).
